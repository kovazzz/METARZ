body {
    text-align: center;
    font-family: Arial, sans-serif;
}

.grid {
    display: grid;
    grid-template-columns: repeat(3, 100px);
    gap: 10px;
    justify-content: center;
    margin: 20px auto;
}

.grid-cell {
    width: 100px;
    height: 50px;
    text-align: center;
    font-size: 16px;
}
